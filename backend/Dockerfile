# Simple Dockerfile for the FastAPI backend
# - Installs Python deps
# - Copies source and .env
# - Runs Uvicorn on port 8000

FROM python:3.11-slim

# Install system deps (optional minimal)
RUN apt-get update \
    && apt-get install -y --no-install-recommends build-essential \
    && rm -rf /var/lib/apt/lists/*

# Set working directory
WORKDIR /app

# Install Python dependencies first for better layer caching
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

# Copy app source (includes .env)
COPY . .

# Expose port
EXPOSE 8000

# Default command: run the FastAPI app with Uvicorn
# server.py already has app instance named `app`
CMD ["uvicorn", "server:app", "--host", "0.0.0.0", "--port", "8000"]

